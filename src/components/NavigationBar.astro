---
import Hamburger from "./Hamburger.astro";
import logo from "../assets/Nameplate 2023_180.png";
import logoRetina from "../assets/Nameplate 2023_retina.png";

const Navbar = {
  logo: logo,
  logoRetina: logoRetina,
  links: [
    { text: "Home", url: "/" },
    { text: "Latest", url: "/latest" },
    { text: "Gallery", url: "/gallery" },
    { text: "Prints", url: "/prints" },
    { text: "About", url: "/about" },
    { text: "Contact", url: "/contact" },
  ],
};
---

<nav class="navbar">
  <div class="nav-container">
    <a class="logo" href="/">
      <img src={Navbar.logo.src} alt="Logo" />
    </a>

    <ul class="nav-links">
      <li>
        <a href={Navbar.links[0].url}>{Navbar.links[0].text}</a>
      </li>
      <li>
        <a href={Navbar.links[1].url}>{Navbar.links[1].text}</a>
      </li>
      <li>
        <a href={Navbar.links[2].url}>{Navbar.links[2].text}</a>
      </li>
      <li>
        <a href={Navbar.links[3].url}>{Navbar.links[3].text}</a>
      </li>
      <li>
        <a href={Navbar.links[4].url}>{Navbar.links[4].text}</a>
      </li>
      <li>
        <a href={Navbar.links[5].url}>{Navbar.links[5].text}</a>
      </li>
    </ul>
    <Hamburger />
  </div>
</nav>

<style>
  /* Navbar Styles */

  .navbar {
    position: fixed;
    top: 0;
    width: 100%;
    height: 70px;
    background: none;
    padding: 0 1rem;
    min-height: 5rem;
    background-color: black;
    transition: all 0.5s;
    z-index: 2;
  }

  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: 1200px;
    margin: 1rem auto;
    padding: 0 1rem;
  }

  .logo img {
    max-width: 180px;
    height: auto;
    transition: all 0.2s linear;
    vertical-align: middle;
  }

  .nav-links {
    display: flex;
    flex-wrap: nowrap;
    justify-content: flex-end;
    margin: 0;
  }

  .nav-links li {
    list-style: none;
    padding: 0.5rem 1rem;
    cursor: pointer;
    transition: all 0.2s ease-in-out;
  }

  .nav-links li a {
    color: var(--secondary);
    text-decoration: none;
    font-size: var(--font-size-4);
    font-family: var(--font-primary);
    font-weight: 600;
    transition: all 0.3s ease-in-out;
    transform: scale(1);
  }

  .nav-links li a:hover {
    color: var(--color-gold);
  }

  .nav-links li:hover {
    transform: scale(1.2);
  }

  .expanded {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100vh;
    background-color: var(--background-light);
    position: absolute;
    top: 0;
    left: 0;
    z-index: 1;
    padding: 0;
  }

  .expanded li {
    padding: 0.5rem 1rem;
    border-radius: 0.25rem;
    cursor: pointer;
    box-shadow: none;
  }

  .expanded li a {
    font-size: 3rem;
    line-height: 5rem;
  }

  .hide-nav {
    top: -5rem;
  }

  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }

    .nav-links.expanded {
      display: flex;
    }
    .navbar .logo .logo-text {
      font-size: var(--font-size-2);
    }
  }
</style>
